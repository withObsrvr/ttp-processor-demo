# Contract Events Service
CONTRACT_EVENTS_SERVICE_ADDRESS=localhost:50053

# PostgreSQL Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5433  # Docker compose maps to 5433 on host
POSTGRES_DB=contract_events
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_SSLMODE=disable

# Filtering (Optional)
# FILTER_CONTRACT_IDS=CABC123...,CDEF456...
# FILTER_EVENT_TYPES=transfer,mint,burn
# INCLUDE_FAILED=false
